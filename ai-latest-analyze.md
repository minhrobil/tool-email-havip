The user wants me to create a comprehensive local Windows tool that reads emails from Outlook Web/Microsoft 365, downloads attachments, parses data from PDFs, and writes to Excel files. This is a substantial project. Let me plan the implementation and then create all the files.
Let me first check if there's an available agent that would be better suited for this task, then proceed with the implementation.
The available agent is "Plan" - for researching and outlining multi-step plans. Since the user wants actual implementation, I should do it myself.
Let me design this project:
Project Name: cong-van-processor or outlook-cong-van-tool
I'm structuring this with separate modules for authentication using MSAL, Microsoft Graph API integration, mail and attachment handling, PDF parsing with regex rules, Excel operations, deduplication logic, logging, a tkinter GUI, configuration management, and Task Scheduler integration to tie everything together. Continuing with the rest of the directory structure for attachments, parsing, Excel output, deduplication, folder management, GUI, and test modules.
Now I'm planning out the core implementation. I need to set up authentication using MSAL with device code or browser flow, caching tokens locally for reuse. The Graph client wrapper will handle the API calls with pagination and rate limiting built in. For the mail reader, I'll search for the "Công văn" folder case-insensitively and pull all emails with proper pagination. The attachment downloader needs to handle filename collisions gracefully, and the PDF parser will extract text from each document.
I'm organizing the parsing logic into modular regex-based rules that can be tested independently, then writing the extracted data to Excel with separate DATA and META sheets. The dedup manager will track processed emails using both technical identifiers and business keys stored in a JSON file. I'll compute daily folder paths from email received dates and create them as needed, then tie everything together with a simple tkinter GUI that provides buttons and a log display.
Now I'm setting up the project structure in the Repositories directory and configuring the necessary files, starting with a config.json that contains Azure authentication details, mail folder settings, output paths for the network drive, and processing preferences like attachment handling and logging levels.
Writing the full implementation... Now I'm laying out the complete project structure with all the necessary modules organized by functionality—authentication, graph API integration, mail handling, PDF parsing, Excel output, deduplication, folder management, and the GUI. I'll start implementing each file to bring this together. Now I'm looking at the parsing layer with regex patterns to extract Vietnamese document metadata like document numbers and deadlines, then converting PDFs to text. The Excel writer handles both data and metadata sheets, while a deduplication manager tracks processed emails using a JSON file. The daily folder utility manages file organization by date, and the main email processor orchestrates everything together.
For the interface, I'm building a tkinter GUI with sign-in, mail scanning, and folder navigation buttons plus a log display, with a headless mode option via command-line flag. The test suite covers parsing rules, deduplication logic, and folder date handling. For the Graph API integration, I need to handle paginated folder listings, navigate the folder hierarchy, retrieve messages and their attachments, then download the files.
Now I'm thinking through the attachment download strategy—the API returns contentBytes as base64 for smaller files, but for larger attachments I should use the $value endpoint to get raw bytes directly. The efficient approach is to first fetch message metadata to check the hasAttachments flag, then only fetch attachment details for messages that actually have them.
For the OAuth flow in a desktop app, I should use the interactive redirect flow since MSAL handles it well with a local redirect URI. The user registers an Azure AD app, sets the client_id in config, and on first run the browser opens for sign-in—after that the cached token handles subsequent runs.
Now I'm setting up the implementation with all the necessary files...
Building the config module... Defining configuration properties...
Now I'm mapping out the full implementation strategy. For the GUI, I'll build a tkinter interface with authentication, mail scanning, and folder access buttons, plus a status display and log viewer. The email processor will authenticate, locate the "Công văn" folder, iterate through messages to extract dates and organize them into daily folders, deduplicate based on prior processing, download attachments, parse the email and PDF data, write everything to Excel, and track what's been processed.
I'm planning the project structure with configuration files, authentication modules, email processing logic, and utility functions spread across multiple files to keep things organized. I've got a lot of files to create here. Let me work through the implementation systematically, starting with the authentication layer using MSAL to handle token acquisition and caching. Then I'll set up the Graph client with proper rate limiting and pagination support, followed by the parsing rules for the công văn documents.
Now I'm defining the regex patterns to extract key fields from the documents—the công văn number, issue date in Vietnamese format, and the application number. Now I'm implementing the classification logic for document types, mapping various Vietnamese phrases to standardized categories like "Từ chối toàn bộ", "Cấp toàn bộ", and different audit result notifications.
Setting up the project structure and Excel schema with columns for tracking incoming documents—including receipt date, folder organization, sender information, attachments, document numbers, document types, deadlines, and case numbers.
Now I'm defining the META sheet to track technical metadata like message IDs and processing timestamps, plus a deduplication system using JSON files to store processed message identifiers per daily folder.
Writing the implementation code... I'm debating between customtkinter for a polished interface versus standard tkinter for simplicity and fewer dependencies—I'll go with standard tkinter to keep packaging straightforward. Now I'm mapping out the full project structure with the entry point, batch scripts for launching and scheduling, and build automation.
Organizing the source modules... Now I'm laying out the rest of the project structure with the GUI module and test suite, then setting up the dependencies and configuration file for the email processing tool. Finishing up the configuration with processing and logging settings, then moving into the main entry point script that handles command-line arguments for headless mode and custom config paths. Now I'm setting up the configuration loader that reads from a JSON file, with a fallback to a default path if none is provided, and it'll raise an error if the config file doesn't exist.
Continuing with the Azure authentication properties...
I'm defining the authority URL using the tenant ID, then setting up the default scopes for mail access. Moving on to the mail configuration properties like the folder name and page size for fetching emails, and finally the output settings for where to save files and what to name the Excel export. Setting up the logger module...
Now I'm adding the file handler for logging to disk when a log file path is provided, then returning the configured logger. I'm also defining a helper function to retrieve an existing logger by name. Moving on to the authentication module where I'm setting up Microsoft Graph OAuth using MSAL...
Setting up the GraphAuth class to manage authentication with client credentials and token caching... Still writing authentication logic... Finishing up the token cache cleanup and checking authentication status, then moving into the Graph API client implementation with retry logic and pagination support. Still writing the request handler... Writing the binary download and pagination methods...
Now I'm moving into the mail reader module, setting up folder discovery and message retrieval through the Graph API. I'm defining a normalization function for Vietnamese text comparison and starting to build out the MailFolder class to represent email folders.
Defining data models... I'm defining a method that searches through mail folders to find a match by name, normalizing Vietnamese characters for comparison. It iterates through top-level folders from the Graph API, builds a list of candidates, and returns the best match—prioritizing exact matches over case-insensitive ones, with appropriate logging for success or ambiguous results. When multiple child folders match, I'll just use the first one and log a warning about it. After finding the folder, I log its name and ID, then return it. Now I'm setting up the message retrieval function, which will fetch all messages from a folder across multiple pages using specific fields like subject, sender, and received date, ordered from newest to oldest.
Defining attachment handling utilities... Continuing with the AttachmentInfo class, I'm storing the base64-encoded content bytes for smaller attachments and providing a property to check if this is an actual file attachment. The get_content method decodes the base64 data when needed. Now I'm building the AttachmentDownloader class that handles fetching attachment metadata from the Graph API, selecting specific fields like ID, name, content type, size, and whether it's inline.
For the download_all method, I'm iterating through each attachment, sanitizing the filename, and determining a unique path to avoid overwrites. If the attachment content wasn't included in the metadata response (meaning it's large), I'm fetching it separately via the $value endpoint. I write the content to disk and log the result, handling any errors gracefully.
Now I'm moving into the parsing rules module, which handles Vietnamese document parsing using regex patterns to extract structured information like document reference numbers. Writing extraction functions... Writing extraction functions... Still writing classification rules... Finishing up the classification rules for document types, then moving into a function to extract the document form type.
Now I'm checking the text for specific intellectual property categories like trademarks, patents, industrial designs, utility solutions, and geographical indications to identify what kind of application this is.
Building extraction functions... Writing PDF extraction logic... I'm setting up the Excel writer module that handles creating and updating the SO CONG VAN DEN-LIENDO.xlsx file with DATA and META sheets, importing the necessary libraries like openpyxl for spreadsheet manipulation and defining the column structure for the DATA sheet.
Defining the META sheet columns... Setting up the Excel workbook structure... Writing the row data... Writing Excel file handling... I'm setting up the deduplication manager to track processed emails using a daily folder structure with a _processed.json file, using both technical keys like internet_message_id and business keys like so_don to identify duplicates. The manager loads existing records on initialization and indexes them by all possible keys for quick lookup. Checking for duplicates based on priority order...
Now I'm implementing the registration method to store processed emails, creating a dedup record with all the identifiers and saving it to the records dictionary, then persisting to disk.
I'm also setting up a utility module for daily folder routing that parses the email received datetime from the Graph API format. I'm writing helper functions to handle datetime parsing and folder path generation. The first function converts UTC timestamps to local time and formats them as folder names, while the second function constructs the full daily folder path by combining the root directory with the formatted date derived from the email's received timestamp.
Still writing folder management code... I'm importing the parsing utilities and setting up the main processor class. I'm defining a ProcessResult dataclass to track the outcome of each processing run, with fields for counting successes, duplicates, items needing review, and errors, along with a method to generate a summary report.
Continuing with the EmailProcessor class...
I'm setting up a helper method to log progress messages to both the logger and the progress callback, then implementing the main run method that orchestrates the entire pipeline. First, I'm authenticating with Microsoft Graph to get an access token, handling any login failures by recording them in the result. Once authenticated, I'm initializing the client, mail reader, and attachment downloader components to begin processing emails.
Now I'm searching for the specified folder in the mailbox, logging its details once found, then retrieving all messages from that folder. If no messages exist, I return early with an empty result. Otherwise, I'm about to iterate through each message to process them.
For each message, I'm logging progress with the current index and subject line, then calling the processing function while catching any exceptions that occur. After all messages are handled, I log a summary and return the result.
In the _process_message method, I'm determining the daily folder path based on the message's received date and the configured date format, then handling cases where the date cannot be determined.
Next, I'm setting up a deduplication manager for that folder and performing a quick pre-check using message IDs to skip any duplicates that have already been processed.
Downloading attachments... Getting the order number from the parsed data, then checking if this is a duplicate using the message ID, internet message ID, and order number. If it's a duplicate, I log it and skip processing. Otherwise, I'm preparing the Excel row by extracting the folder name, received date, and determining the status based on whether the email needs review.
Building the data row with all the extracted fields... I'm registering this message in the deduplication system with its key identifiers and attachment info, then logging whether it needs review or succeeded. After that, I'm writing the run log entry for this folder to track the processing outcome.
Now I'm looking at the GUI application file that provides a simple tkinter interface for users to interact with the Công Văn Processor... I'm setting up the main window with the title, geometry, and background color, then configuring the close handler and initializing the status display. After that, I'm building the UI and attempting to load the configuration file, catching any file not found errors to display appropriate warnings.
Building the UI layout... Setting up the action buttons... Still writing UI components... Writing the logging and status methods... Writing the login handler... Setting up the email processor with the loaded configuration and logger, then running the scan to process emails and display the results—success count, duplicates, and any errors that occurred. If something goes wrong, I'll log the exception and show an error status to the user.
Now I'm wrapping up the scan operation by resetting the running flag and re-enabling the buttons, then launching this whole process on a background thread so the UI stays responsive. After that, I'm handling the folder opening functionality—when the user clicks to open the root folder, I check if the configuration exists and the folder path is valid before launching it in Windows Explorer, with appropriate error messages if something fails. Finally, I'm starting to handle the window close event, checking whether a scan is currently running.
Writing test suite... Writing test cases... Writing test cases... Writing test cases... Writing test assertions... Writing test cases... Writing test cases... Writing test cases... Writing test cases...
Now I'm moving on to the README and batch scripts for running the application. I'm creating a comprehensive README file, then setting up batch scripts for both interactive and headless execution modes, plus a scheduler setup script to register the task in Windows Task Scheduler.
Still writing batch scripts... Configuring the PyInstaller executable settings...
Now I'm ready to start creating all the necessary files for this implementation.
I need to fix a few compatibility issues: removing the Linux-only fcntl import from the Excel writer, switching to List[Path] type hints for Python 3.9+ compatibility, and ensuring all __init__.py files exist. I also need to handle the edge case in the Excel writer where _get_next_row should correctly return row 2 when the sheet only has headers.